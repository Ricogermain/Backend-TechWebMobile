# Neon: URL poolée (hôte contenant -pooler), utilisée par l'API en production.
DATABASE_URL="ep-wispy-base-ava46qr8-pooler.c-11.us-east-1.aws.neon.tech"

# Neon: URL directe (sans -pooler), utilisée par Prisma migrate et le seed.
DIRECT_URL="ep-wispy-base-ava46qr8.c-11.us-east-1.aws.neon.tech"

# Générer une valeur longue et aléatoire en production.
JWT_SECRET="change-me-with-a-long-random-secret"
PORT=3000
